import React from 'react'
import "./sectionTitle.css"
const SectionTitle = ({ title }: any) => {
    return (
        <h2 className="section-title-big">{title}</h2>
    )
}

export default SectionTitle